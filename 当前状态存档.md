# 当前状态存档（技术快照）

最近更新：2026-05-07 17:47 CST  
发布版本：`49225e9 Capture recognition review samples`

> 用途：给后续 coding agent / 自己回顾时，快速对齐「此刻仓库里真实在跑什么」。  
> 更新说明：不含任何密钥；环境变量仅在 Vercel 配置。

---

## 1. 主入口与页面

- **推荐唯一主入口**：`pantry.html`（部署后访问：`…/pantry.html`）
- `pantry-v4_1.html`：历史副本，建议不再作为主开发入口；识别链路已与后端代理对齐，避免误开旧页导致行为不一致

---

## 2. 后端（Vercel Serverless）

目录：`api/`

| 路径 | 作用 |
|------|------|
| `api/admin/backups.js` | 管理后台查看和恢复家庭备份 |
| `api/admin/recognition-reviews.js` | 管理后台查看已授权识别样本 |
| `api/admin/usage.js` | 管理后台使用看板数据 |
| `api/families/ensure.js` | 匿名 `clientId` 确保/创建家庭并绑定 |
| `api/families/[familyId].js` | `GET` 拉取家庭数据；`PUT` 带版本号写入（409 冲突） |
| `api/families/[familyId]/join-token.js` | 生成加入家庭的 token（v2） |
| `api/families/join.js` | 新设备用 token 加入家庭（v2） |
| `api/openrouter.js` | 代理 OpenRouter，前端不持有平台 Key |
| `api/recognition-review.js` | 前端回传识别解析结果、最终采纳结果 |
| `api/usage-event.js` | 客户端通用埋点 |

**已修复问题（重要）**：`api/openrouter.js` 曾存在重复 `export default handler` 导致线上 500；当前为单一实现，错误体统一为 `{ error: { message } }` 便于前端解析。

**内测防护**：`api/openrouter.js` 已要求合法 `X-Client-Id` / `X-Family-Id`，校验客户端属于该家庭，只允许图片识别请求，并对 client / family / IP 做限流。

---

## 3. 数据库（Supabase）

- 脚本：`supabase/schema.sql`
- **表名带 `pantry_` 前缀**（与同库其他项目隔离）：
  - `pantry_families`
  - `pantry_clients`
  - `pantry_join_tokens`
  - `pantry_recognition_usage`
  - `pantry_usage_events`
  - `pantry_family_backups`
  - `pantry_recognition_reviews`
- 触发器：`trg_pantry_families_updated_at`（条件创建，避免误删提示）

---

## 4. 前端云同步（v1）

在 `pantry.html` 中：

- 本地生成并持久化 `clientId`
- 启动时 `POST /api/families/ensure` 绑定/创建家庭
- `save()` / `saveLocations()` 防抖同步到 `PUT /api/families/:familyId`
- 409 时拉取服务端数据并提示用户

---

## 5. AI 识别

- `config.js` 中 `OPENROUTER_CONFIG.apiUrl` 指向 **`/api/openrouter`**
- 用户**无需**在浏览器配置平台 Key（设置里旧入口可后续下线或标为高级调试）
- 当前视觉模型走 Vercel 环境变量里的 OpenRouter Key，模型列表在 `config.js`
- 录入页已有“拍照示例”：正面标签、保质期面、容易失败
- 第一次拍照识别前会弹出“识别样本授权”
- 用户同意后才保存压缩照片、模型原文、解析结果；确认录入后回传最终采纳结果和字段改动

---

## 6. 管理后台

- 后台入口：`https://pantry-mng.vercel.app/admin.html`
- 需要 `PANTRY_ADMIN_TOKEN`
- 看板包括：
  - 家庭 / 客户端 / 活跃 / 事件统计
  - OpenRouter 请求和识别错误
  - 备份恢复
  - 识别样本：图片、模型原文、解析结果、最终录入、字段改动

---

## 7. Vercel 环境变量（运维核对清单）

需在 Vercel 项目配置（名称建议一致）：

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `OPENROUTER_API_KEY`
- `PANTRY_ADMIN_TOKEN`

---

## 8. 对外发布状态

- 生产域名：`https://pantry-mng.vercel.app`
- 最新生产部署已由 Vercel CLI 确认 Ready，并 alias 到生产域名
- 最新代码已推送 GitHub `main`
- 发布前验证：
  - API JS 语法检查通过
  - HTML 内联脚本检查通过
  - `supabase.pantry_recognition_reviews` 已创建，当前初始样本数为 0
  - 本地 Vercel dev 验证了无身份请求 `/api/openrouter` 返回 401，非法识别回传返回 400
- 注意：当前终端偶发无法直连生产域名，表现为 DNS / connection reset；Vercel CLI 侧部署状态正常

---

## 9. 相关文档索引

- 总体方案：`后端化产品技术方案.md`
- 仓库说明：`README.md`

---

## 10. 已知注意点

- 根路径 `/` 若 404：多为未配置 `index.html`，需显式访问 `pantry.html` 或在平台配置重定向
- 共享数据库时务必使用 `pantry_*` 表，勿与其他项目表名冲突
- 识别样本会保存真实商品包装照片，仅用于内测用户同意后的复盘；正式扩大范围前建议补隐私说明和样本保留期限
